{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distrDir": "dist" }
    }
  ],
  "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}
